

//User function Template for Java
class Solution{
    static int sumOfDigits(int N) {
        // code here
         int sum = 0;
        int digit;
        
        while(N > 0){
            digit = N % 10;
            sum = sum + digit;
            N = N/10;
        }
        return sum;
    }
}
       
